import stylefunction, {
  recordStyleLayer,
  renderTransparent,
} from './stylefunction.js';
import {
  apply,
  applyBackground,
  applyStyle,
  getFeatureState,
  setFeatureState,
} from './apply.js';

export {
  apply as default,
  apply,
  applyBackground,
  applyStyle,
  setFeatureState,
  getFeatureState,
  stylefunction,
  recordStyleLayer,
  renderTransparent,
};
